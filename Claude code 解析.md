# Claude code 解析

### 上下文压缩

![image-20260824171754927](C:\Users\CC\AppData\Roaming\Typora\typora-user-images\image-20260824171754927.png)

这种设计的另一个好处是各层相互协调。第2层Snip会告诉第5层「我已经释放了多少 Token」，避免重复压缩。第4层Context
Collapse在第5层之前运行，如果它够用了，第5层就不触发。每一层都在为下一层「减负」。

- **第一层**：它的逻辑很简单:如果单个工具的结果超过约50KB，就把完整内容写到磁盘上，在消息里只留一个2KB的预览摘要。这样模型还是能看到文件的大概内容(前2KB)，但不会撑爆上下文；除了单个工具的限制，还有一个消息级的总量控制，同一条消息里所有工具结果的总大小不能超过200KB。如果超了，系统会挑出最大的那几个结果存磁盘，直到总量降到限制以内。

- **第二层**： Snip层是最「粗暴」但也最高效的一层，直接把对话开头的一批老消息移除掉，然后插入一个边界标记告诉模型「这之前的内容已经被清理了」。并且snip层会将这次处理释放了多少token这件事情传递给第五层，避免两层同时做无意义的压缩

- **第三层**：Claude Code怎么做?Micro-Compact层的核心思想是时间哀减:越老的工具结果越不重要，可以被裁剪。但是，不是所有工具的结果都能裁剪: 可以被裁的，都是「可重新获取」的工具，Read的结果可以再读一次，Bash的输出可以再执行一次，搜索结果可以再搜一次。但AgentTool (子Agent 的输出)、TaskTool (任务状态)这类工具的结果永远不会被裁剪，因为子Agent 的推理过程是不可重复的，砍掉就真的丢了。

- **第四层**：前面三层都是「写时压缩」，直接修改消息列表，把内容替换掉或删掉。但Context Collapse不修改原始消息，它只在调用API的那一刻，动态计算一个「压缩视图」给模型看。90%上下文窗口:主动开始分段压缩旧消息(预留缓冲区)；95%上下文窗口:紧急压缩更多内容(留足API响应空间) 【第四层与第五层属于二选一】

- **第五层**（距离上下文窗口限额20k【给摘要留出来的】+13k【设置的绝对值安全栈】触发）：

  1. 第一步:生成摘要。调用大模型，把整段对话总结成一段结构化摘要。Claude Code用一个精心设计的Prompt要求模型按多个维度来总结: 用户的主要请求和意图、关键技术概念、涉及的文件和代码片段、遇到的错误和修复方案、问题解决过程、用户的所有消息(不能遗漏任何一条)、待完成的任务、当前工作状态、建议的下一步。（记得这么细方便压缩后模型靠这段摘要来回复数据）

  2. 第二步:替换旧消息。把压缩边界之前的**所有消息**删掉，替换为刚才生成的摘要。同时插入一条边界标记消息，记录压缩前的Token数，这次压缩是自动还是手动等等，相当于打个时间戳。

  3. 第三步：Post-Compact Restoration(压缩后恢复)。这是整个流程中最关键的一步，压缩完不是就完了，还要主动恢复最重要的上下文。找出最近访问过的文件，按最后访问时间排序，挑选出5个文件内容重新注入，同时恢复活跃的skill。主要是担心压缩后的模型失忆了，不记得刚才度过的文件内容

     <img src="C:\Users\CC\AppData\Roaming\Typora\typora-user-images\image-20260824215023376.png" alt="image-20260824215023376" style="zoom:83%;" />

### 长期记忆存储

老派存储记忆方式：滑动窗口，对话摘要，向量检索【召回不稳定，稳定成本高】，分层存储【分成常驻上下文，最近可召回对话，远期归档三层】

Claude code分为两层：

- 静态层：

​	<img src="C:\Users\CC\AppData\Roaming\Typora\typora-user-images\image-20260825145738367.png" alt="image-20260825145738367" style="zoom:70%;" />

- **动态记忆memory系统层**：

  - 记忆memory系统「记该记的，不记能推导的」。通过四类型封闭集合加上排除清单，把记忆控制在有价值的范围内，防止它膨胀成一个什么都往里塞的垃圾堆。**四类记忆**：

    - user：用户画像: 记住用户是谁，擅长什么，知识水平如何

    - feedback：行为反馈：记住用户说过的--该做什么、不该做什么，并且记住why(为什么) 和 how to apply(怎么应用)

    - project：项目动态:在做什么、截止日期（得把相对日期转成绝对日期）、协作信息

    - reference：外部指针:哪里能找到什么信息

    - 不记代码模式，项目架构，文件结构，这些能通过grep，git，claude.md能获取到的东西，还有就是能从当前代码推导出来的信息也不存，因为代码随时在变

  - 「存索引，按需加载详情」。MEMORY.md 作为轻量索引始终常驻在System Prompt里，但每条记忆的具体内容是独立文件（长得像skill），用到的时候才加载。这样既让Agent知道有哪些记忆可用，又不会撑爆上下文。主对话循环结束之后，后台单独跑一个代理来抽取记忆

  - 「用小模型做秘书，大模型做决策」。把所有文件头部信息拼接在一起给Sonnet，Sonnet负责并行预取和选择记忆，只返回文件列表，Opus只管做决策，加上陈旧度检测机制，实现了零延迟、低成本、高可靠。
  - 用 <system-reminder> 标签包裹后注入到上下文中。为防止记忆老化，两天以前的记忆需要警告

### Agent工作模式

- 采用**Tool-Use Loop**, 相比较于ReAct少了thought思考过程，extended thinking让推理在模型内部完成，选择信任模型的推理能力，把应用层框架做的尽可能简单
- 采用**Plan Mode**：先plan再执行，简单任务不执行，进入到plan mode之后权限降为只读，plan完之后用户审批之后，权限恢复为之前的default模式

### 主循环query是如何实现的

- Agent怎么知道主循环是否能停下来：**就看模型返回里有没有 `tool_use` 块**。但是停下来也不代表正常完成。一个工业级的 Agent 主循环，它要兜住的从来不是「正常退出」，而是十几种「异常退出」。
- 模型一边流式输出，主循环就一边监听。一旦识别出一个完整的 `tool_use` 块，立刻把它丢给后台开始执行，不等模型流完。等模型流完，最早开始执行的那个工具，可能已经回结果了。

Claude Code主循环背后有**四个设计哲学**:

1. 边干边吐，让感知前置。用异步生成器把每个中间事件即时抛出去，用户体感「响应快」就是这么来的。
2. 状态显式管理。该计数的字段全部摊在一个状态对象里，不藏在闭包变量里。能调试的状态才是可靠的状态。
3. 引擎层不掺业务。主循环不知道工具具体在干嘛，新增能力零侵入，只需要往工具池里塞一个新工具就完事。
4. 错误恢复优先于优雅。每个失败路径都有兜底。宁可丑陋地合成一个假的工具结果（这个工具没执行成功）塞回去，也不能优雅地崩溃。

### System Prompt构造

第一个是「先给范围再画红线」。比如安全约束那段，它不是一上来就说「不准做这不准做那」，而是先说「安全测试、CTF挑战这些可以做」，然后再说「DoS、供应链攻击这些不能做」。这比写十句「不准XX」管用得多，因为模型拿到了判断标准，而不是一堆模糊的禁令。

第二个是「用两个维度把风险分出层次」。Claude Code判断一个操作安不安全，不看它「看起来危不危险」，而是看两件事:这操作能撤回吗?会影响别人吗?改本地代码当然能撤回、只影响自己，直接放行。gitpush撒不回来、别人能看到，那就得确认。这个思路比笼统的「危险/安全」二分法精细太多了。

第三个是「静态内容和动态内容用分割线隔开」。那条分割线不是随便画的，它把所有用户都一样的部分和因人而异的部分切开了。这样做的好处是，分割线之上的内容可以被全球所有用户共享缓存，如果两次请求的prompt前缀完全相同，每次APl调用能省90%的费用。一个看似简单的排版调整，背后是实打实的成本优化。

### multiagent

![image-20260729173417381](C:\Users\CC\AppData\Roaming\Typora\typora-user-images\image-20260729173417381.png)

![image-20260729173438723](C:\Users\CC\AppData\Roaming\Typora\typora-user-images\image-20260729173438723.png)

### 为什么代码检索用grep而不用RAG

简单定向搜索（你知道要找啥）就直接用 Grep/Glob/Read；开放式探索（你不太确定要找啥）就派 Explore 子 agent。临界点大概是「预期超过 3 次查询」。

1. 第一步，先用 Glob 找候选文件，比如 **/*login*.{ts,tsx,js}，可能拉回来 5 个候选文件。

2. 第二步，用 Grep 在这些文件里搜关键字，比如 passport|auth|login，定位到具体的几个命中行。

   - grep单独包装成一个工具，属于工具类，并不等同于常规的rm，git命令。可以确保输出格式可控，并且使用的底层是ripgrep，性能很好

3. 第三步，用 Read 读命中文件的相关行段，看具体实现。

   ![image-20260825114911776](C:\Users\CC\AppData\Roaming\Typora\typora-user-images\image-20260825114911776.png)

![image-20260729173643540](C:\Users\CC\AppData\Roaming\Typora\typora-user-images\image-20260729173643540.png)

### MultiAgent

- 父子型。主 agent 处理整个任务，遇到某个子问题时派一个 subagent 出去搞定，拿结果回来接着干。这是最常见的，Claude Code 里的 Task 工具就是这种。
- 平级协作型。几个 agent 职责对等，通过共享状态或者消息互相协作。不过这种在工程上比较难落地，状态同步很麻烦。
- 主从型（Coordinator-Worker）。有一个专门的「协调者 agent」，**它自己不干活**，只负责派 worker、收结果、做合成。worker 之间互不通信，全靠协调者调度。这种是高并发场景的标配。

#### Subagent的隔离机制

- 每个状态单独决策。父agent->子agent：读文件缓存克隆（避免污染），对子agent写全局状态关掉（避免两边抢），父agent任务注册通路保留（不然孤儿进程没人回收），子agent深度计数 +1（可追踪，防失控嵌套）
- 工具权限要分级管控：**全局黑名单**（防子agent递归、防乱问用户），**类型黑名单**（自定义 agent 更严），**异步白名单**（后台 agent 只能用事先定义好的小批量工具）

#### 父子agent怎么通信

- 默认形态：父 agent 派子 agent 出去、等结果，长任务转后台后子 agent 回头发完成通知。消息通路基本是子→父 单向通知，父 agent 不能中途插话。同步执行，等待两分钟后才转为后台
- agent-teams团队模式：父 → 子：写入子 agent 的消息队列，子 agent 下一轮循环自己读取。子 → 父：把完成通知包装成 XML 消息，伪装成**用户消息**注入父 agent 对话。没有直接函数调用，没有锁，没有回调地狱，全靠读写共享的任务状态和消息队列。

#### Fork agent

派一个子 agent 出去干活，但这个子 agent 的 API 请求前缀跟父 agent 一模一样，让 Anthropic 那边一看：「哦这个前缀我认识」，走缓存。适用场景：希望子 agent 完全继承父 agent 的整个上下文，但又不希望污染父 agent 的主循环。如果任务有明确的专业分工，那就用常规subagent。Fork机制和coordinator机制是互斥的，主要是为了节省token的消耗

#### Coordinator

- 派 worker 的工具：派一个新 worker 出去干某件具体的活，派完立刻返回 worker 的 ID。
- 创建/解散团队的工具：批量管理 worker 组。
- 给 worker 发消息的工具：给已经派出去的 worker 发后续指令（也就是前面讲的 SendMessage），因为 worker 的上下文还在，续命比重新派一个更省钱。
- 合成最终输出的工具：协调者合成完答案后，通过这个工具把最终回复交给用户。是**协调者亲自做**，这是协调者存在的意义：**必须**理解全局，做决策，不能转发。
- 停止 worker 的工具：当协调者意识到某个 worker 跑错方向时，把它停掉省 token。

coordinator的专有工具不给worker用，形成递归防护，让系统保持扁平形态

##### Continue vs Spawn：老 worker 还是新 worker？：

如果新任务跟 worker 现有上下文高度相关（比如刚查的文件现在要改），续命老 worker，因为它已经「知道」那些文件了。
如果新任务跟 worker 现有上下文没关系，或者之前 worker 的工作走偏了，派新 worker，避免旧上下文干扰判断。