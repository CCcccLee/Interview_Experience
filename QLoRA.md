# QLoRA

下面是10个由浅入深的问题。回答采用候选人口吻，并严格限定项目结论：97.71%来自175条同分布合成测试集，不能直接代表生产环境。

## Q1：先介绍一下这个项目解决了什么问题，你具体负责什么？

这个项目解决的是通信运维场景中的“现象节点匹配”问题。输入是一条用户故障描述，以及检索系统召回的一组候选现象节点；模型需要从中选择唯一匹配节点。如果信息不足、存在多个现象或者候选中没有正确节点，则输出 `NO_MATCH`。

我主要负责三个部分：第一是把图谱节点匹配转换成指令微调任务；第二是构造同层、跨层困难负样本和开放集拒识样本；第三是使用Qwen2.5-3B-Instruct进行QLoRA微调，并设计严格准确率、拒识F1和Node-Holdout实验验证效果。

模型只允许输出两种JSON：

```
{"result":"MATCHED","node_id":"PH_xxx"}
{"result":"NO_MATCH","node_id":null}
```

## Q2：为什么使用大语言模型，而不是直接做文本分类？

因为节点集合不是完全固定的。传统分类模型会把每个节点ID看作一个类别，新增节点后通常需要扩充分类头并重新训练，很难处理训练期未见节点。

我的方案把候选节点的名称、实体类型和描述作为输入，让模型学习“如何比较Query和节点”，而不是单纯记忆“Query到类别ID”的映射。因此新增节点只要能够被检索出来并提供节点文本，模型就有机会直接判断。

另外，这个任务不只是相似度排序，还包含唯一性约束和开放集拒识。例如“链路中断”和“物理链路中断”词面非常接近，但主体层级和限定条件不同；如果候选都不完全满足条件，还必须拒绝。单纯的向量相似度或分类模型较难统一处理这些规则。

## Q3：为什么选择Qwen2.5-3B-Instruct，而不是1.5B、7B或者BERT？

我主要考虑了任务能力、中文支持、结构化输出和硬件成本。

Qwen2.5-Instruct具备较好的中文理解和JSON指令遵循能力。这个任务属于候选集上的细粒度判别，不需要特别强的开放式生成能力，因此3B参数量基本能够覆盖需求。

硬件方面只有一张V100。1.5B虽然成本更低，但在困难负样本、主体层级识别和开放集拒识上可能存在容量不足；7B的训练和推理成本更高，在V100上也会进一步压缩batch和吞吐。因此我选择3B作为效果与成本之间的折中，再通过QLoRA降低显存占用。

需要说明的是，我没有完成1.5B、3B、7B的全量对照实验，所以不会宣称3B是理论最优，只能说它是在当前任务和资源约束下合理的工程选择。

## Q4：QLoRA的核心原理是什么？为什么NF4适合这个项目？

QLoRA首先将基础模型权重量化到4-bit并冻结，然后只训练插入线性层中的LoRA低秩矩阵。对于原始权重更新，可以近似表示为：

```
[
W' = W + \frac{\alpha}{r}BA
]
```



其中基础权重 (W) 不更新，只优化低秩矩阵 (A) 和 (B)。这样显著减少了可训练参数、优化器状态和梯度的显存占用。

我使用的是NF4加double quantization。NF4针对近似正态分布的预训练权重设计，比普通的均匀INT4更适合保存权重信息；double quantization会继续量化量化尺度，从而进一步节省显存。

这里需要区分存储精度和计算精度：基础权重以NF4形式存储，但V100不原生支持BF16，因此实际计算采用FP16。

## Q5：LoRA加到了哪些层？为什么选择all-linear？

实际包含注意力模块的：

- `q_proj`
- `k_proj`
- `v_proj`
- `o_proj`

以及MLP模块的：

- `gate_proj`
- `up_proj`
- `down_proj`

没有训练Embedding、LayerNorm和完整的`lm_head`。

选择`all-linear`是因为这个任务不只是调整输出格式，还需要改变模型对故障主体、异常属性、层级关系和候选节点的内部表示。注意力投影负责建立Query与候选信息之间的关系，MLP投影负责领域特征的非线性变换。相比只适配`q_proj`和`v_proj`，`all-linear`的可训练参数更多，但基础模型已经4-bit冻结，而且LoRA rank只有16，因此仍能在单张V100上训练。严格来说，这也是一个工程选择；如果算力充足，我会增加`q/v`、全部注意力层和`all-linear`三组消融实验。

## Q6：为什么LoRA设置为r=16、alpha=32、dropout=0.05？其他训练参数如何确定？

`r=16`是适配能力、显存开销和过拟合风险之间的折中。这个任务的输出空间和决策规则相对受限，数据规模也不算大，没有必要一开始使用很高的rank。

`alpha=32`使LoRA缩放比例为：2

保证低秩更新具有足够影响。`dropout=0.05`主要用于缓解小规模领域数据上的过拟合。

其他核心参数包括：

- 学习率：`2e-4`
- 训练轮数：3
- warmup：5%
- 学习率调度：cosine
- micro batch：1
- 梯度累积：16
- 有效batch：16
- 最大序列长度：1792

1792来自真实token长度统计：平均约1090，P95约1390，最长1654，因此能够覆盖全部样本，又避免使用4096带来的无效计算。

这些参数不是通过大规模网格搜索得到的，而是先根据QLoRA经验和数据统计确定稳定基线，再用验证集损失选择checkpoint。面试中我不会把它描述成“完整调参后的全局最优”。

## Q7：你说的困难负样本具体是什么？为什么效果明显？

普通随机负样本通常和正确节点差异很大，例如“端口丢包”和“单板温度异常”，模型很容易依赖关键词完成区分，学不到真正的决策边界。

我构造了两类主要困难负样本：

第一类是同层困难负样本。候选节点具有相同或相近主体，但异常属性不同，例如“端口流量中断”“端口丢包”“端口误码”。

第二类是跨层困难负样本。异常词相似，但主体或图谱层级不同，例如“业务中断”“链路中断”和“物理链路中断”。

模型必须同时比较主体、属性、异常类型和限定条件，不能只看“中断”“告警”等关键词。

消融实验中，完整模型严格准确率为97.71%，去除困难负样本后下降到85.14%，降低12.57个百分点，McNemar检验的p值为 (2.74\times10^{-5})。这是当前项目中证据最明确的有效组件。

## Q8：开放集拒识是怎么实现的？为什么不用相似度阈值？

我的拒识不是在生成结果后简单加一个相似度阈值，而是把`NO_MATCH`作为显式训练目标。训练数据覆盖几种情况：

- 正确节点不在候选集合中；
- Query包含多个独立现象，无法唯一匹配；
- 候选节点只在关键词上相似，但主体或属性不一致；
- Query信息不足，多个候选都可能成立。

训练时只对assistant的目标JSON计算损失，并将`NO_MATCH`样本按1.5倍进行训练集过采样，使模型能够学习“什么时候不应该强行匹配”。最终测试集包含149条`MATCHED`和26条`NO_MATCH`，模型的`NO_MATCH F1`为96.15%。之所以使用F1，是因为拒识样本占比较低，只看总体准确率可能掩盖模型的误拒绝和漏拒绝。不过过采样的单种子消融只带来了0.57个百分点的变化，当前证据不足以证明1.5倍就是最优比例。更严谨的做法是补充多随机种子实验，并绘制拒识Precision-Recall曲线。

## Q9：97.71%的严格准确率可信吗？如何避免数据泄漏？

这里的严格准确率不是只判断节点ID是否正确，而是要求：

- 输出必须是合法JSON；
- `result`必须正确；
- `node_id`必须正确；
- `NO_MATCH`时`node_id`必须为`null`。

测试集共175条，模型正确171条，严格准确率为97.71%，合法JSON率为100%，Wilson 95%置信区间为94.27%到99.11%。

为了避免只和弱基线比较，我加入了以下对照：

- 基础模型加严格JSON提示：53.71%
- 基础模型加宽松提示：51.43%
- 字符TF-IDF：78.86%
- BM25：76.00%

同时进行了三个随机种子实验，严格准确率分别为97.71%、97.14%和98.29%，均值为97.71%，样本标准差为0.57个百分点。

但这个结果来自同分布合成测试集，只能证明模型在当前数据分布上的效果，不能直接说线上准确率达到97.71%。真实场景还需要匿名真实Query、多人标注一致性和独立冻结测试集验证。

## Q10：Node-Holdout为什么能验证新节点泛化？它有哪些局限？

我从257个活跃节点中按实体类型分层留出51个节点，占19.84%。训练时，这些节点的ID、候选记录和标签都不进入holdout模型训练集，并检查到节点ID泄漏数量为0。

测试集包含这51个未见节点对应的364条样本。模型正确351条，Micro exact accuracy为96.43%，Macro node accuracy为96.71%，JSON有效率为100%。基础模型在相同测试集上的准确率是59.07%。

它验证的是：模型即使没有在微调阶段见过某个节点ID和节点记录，只要推理时能够获得该节点的文本描述，就可以根据学到的匹配规则进行判断。但它不能证明模型完全理解从未见过的通信知识，因为基础模型预训练阶段可能接触过相关概念，而且测试时仍然提供了候选节点文本。因此准确说法应该是“对训练期未见节点的匹配泛化”，而不是“对未知通信知识的零样本泛化”。

如果继续落地，我会采用“检索器Top-K召回＋QLoRA模型精排与拒识”的两阶段架构。现有实验中Top-10检索Recall为97.99%，端到端严格准确率为96.00%，比理想候选集合下降1.71个百分点。后续需要分别优化检索遗漏、主体层级混淆和拒识校准，并在真实生产数据上验证延迟、吞吐和准确率。

## 为什么不训练lm_head?为什么不只训练q_proj,v_proj?

1.基座模型预训练阶段，`lm_head`已经学好了：**什么样的 hidden‑state 对应什么 token**。输出空间虽然表现为JSON，但最终使用的仍然是模型原有词表，输出内容也只有固定状态和节点ID，并不需要增加新token。因此没有必要单独训练完整的 `lm_head`。冻结它可以减少参数量和过拟合风险，LoRA对模型内部表示的调整已经能够改变最终token的生成概率。（hidden‑state：**语义向量，代表含义，不是 token，没有词表概念**；lm_head：做相似度匹配，把语义向量，换算成每个 token 的分数 logits，确认这个语义向量和哪个token的匹配度最高）

2.Transformer 有两大模块：

1. **多头注意力层**：做 token 之间关联、上下文建模（q/v/k/o）
2. **MLP/FFN 层**：对单 token 内部语义做变换、知识记忆、推理，存储大量事实知识。

只训 q+v：**只能修改注意力的检索、信息提取逻辑，完全碰不到 MLP**。 MLP 里面存的领域知识、事实、推理逻辑完全沿用基座预训练的。

> 如果你的任务需要**注入大量新知识、改变内部知识表征**，q+v 能力就到顶了，怎么调 r、lr 效果提升都有限。

all‑linear：既改注意力，又改 MLP；适配器可以同时调整上下文关联 + 内部知识变换，表达能力上限更高，适合领域迁移、持续预训练。

> 举例：把通用模型微调成专业医疗大模型。 q+v 只能学会 “怎么去读医疗文本”，但是很难把大量医疗新知识写入模型；all‑linear 可以在 MLP 中适配专业知识。



## Q25：请详细描述一条样本在QLoRA中的前向和反向传播过程

首先，tokenizer使用Qwen原生Chat Template将`system`、`user`和`assistant`消息拼接并转换为token。

前向传播时，冻结的基础权重以4-bit NF4形式存储。执行线性计算时，bitsandbytes会将当前计算需要的权重块反量化到FP16计算精度，再与输入激活进行矩阵运算。

对于插入LoRA的线性层，输出可以表示为：

[
y = Wx+\frac{\alpha}{r}BAx
]

其中(W)是冻结的4-bit基础权重，(A)和(B)是可训练的LoRA矩阵。

计算损失时，prompt部分的label被设置为`-100`，因此system、Query和候选节点不参与损失，只对assistant输出JSON的token计算交叉熵。

反向传播时，梯度会经过模型计算图传播，但只保存和更新LoRA参数的梯度。基础模型权重被冻结，不会产生参数更新。因此这里不需要通过STE去更新4-bit基础权重；量化权重只参与前向和反向中的数值计算。

每16个micro batch累积一次梯度，完成梯度裁剪后由优化器更新LoRA参数。

## Q26：4-bit权重如何参与计算？会不会所有计算都是4-bit？

不会。4-bit主要是基础权重的存储格式，不代表激活值、梯度和矩阵乘法全部采用4-bit。

本项目中：

- 基础模型权重：NF4存储；
- 基础权重计算：按需反量化到FP16；
- 激活值：FP16混合精度；
- LoRA可训练权重：在V100配置中保持FP32主权重；
- LoRA梯度和优化器更新：由混合精度和GradScaler管理；
- 优化器状态：使用8-bit Paged AdamW压缩。

如果把所有部分都简单理解成4-bit训练是不准确的。QLoRA节省显存的核心，是冻结并量化占比最大的基础权重，同时只更新规模较小的LoRA参数。

## Q27：`prepare_model_for_kbit_training`做了什么？为什么不能直接插入LoRA训练？

它主要负责让量化模型处于适合低比特训练的状态，典型工作包括：

- 冻结基础模型参数；
- 将部分对数值稳定性敏感的模块转换到更稳定的精度；
- 为梯度检查点和输入梯度做好准备；
- 避免对量化基础权重进行普通全参数更新。

如果直接对4-bit加载后的模型插入LoRA，而没有完成这些准备，可能出现参数冻结不完整、梯度无法正常回传或者数值稳定性问题。

我的训练流程是先加载NF4基础模型，再调用`prepare_model_for_kbit_training`，最后通过PEFT配置插入LoRA模块。

## Q28：为什么V100使用FP16，而LoRA参数又被转成FP32？这是否矛盾？

不矛盾，这是混合精度训练中“计算精度”和“参数主副本精度”的区别。

V100是Volta架构，计算能力7.0，不原生支持BF16，因此自动精度策略选择FP16。基础权重反量化和大部分前向计算使用FP16，以获得速度和显存优势。

但部分PyTorch和bitsandbytes组合可能将LoRA参数初始化成BF16，V100上的AMP GradScaler在梯度反缩放时可能因此出现兼容性问题。所以训练脚本会将可训练的LoRA参数统一转为FP32，同时保持冻结的基础权重为4-bit。

这样做的好处是：

- 大部分计算仍然享受FP16的效率；
- LoRA参数更新具有更大的数值范围；
- 避免FP16小梯度下溢；
- GradScaler可以稳定地进行缩放和反缩放。

## Q29：为什么只对assistant答案计算loss？如果对整段对话计算会怎样？

这个任务的训练目标是根据Query和候选节点生成正确决策，而不是让模型学习复述输入。

如果对整段对话计算loss，system指令、Query和候选文本通常占总token的绝大多数。模型的优化目标会被“预测输入文本”主导，而真正重要的`MATCHED/NO_MATCH`和`node_id`只占很小比例。

这可能造成：

- 有效监督信号被稀释；
- 模型更关注复现候选描述；
- 训练loss下降，但严格匹配准确率提升有限；
- 增加无意义的计算和梯度噪声。

因此我先将样本转换成prompt-completion结构，并设置`completion_only_loss=True`。数据整理器把prompt对应label设为`-100`，交叉熵会忽略这些位置，只监督assistant JSON。

需要注意的是，attention并没有屏蔽prompt。模型仍然能够读取system、Query和候选节点，只是这些token不作为预测目标计算损失。

## Q30：micro batch为1、梯度累积16，真的等价于batch size 16吗？

从一次参数更新所聚合的样本数量来看，有效batch是16，但它不与物理batch 16完全等价。

主要差异包括：

- 每个micro batch的dropout掩码不同；
- 序列长度不同导致每个样本贡献的有效token数不同；
  - **如果物理 Batch=16**：梯度主要由 `(8*2048 + 8*128)` 个 Token 共同决定，长文本贡献了 **94%** 的梯度信息。
  - **如果累积 16 步（你的做法）**：模型先看 8 条长文本，再看 8 条短文本。虽然在数学平均值上，长文本的梯度幅值可能和短文本差不多，但**梯度估计的方差（Variance）**完全不同。

- BatchNorm等依赖batch统计的模块会产生差异，不过Transformer通常使用LayerNorm，因此这点影响较小；
- 优化器每16步才更新一次，学习率调度也按optimizer step推进；
- 分布式训练时还会涉及梯度同步方式。

这个项目中样本长度差异较大，因此更严谨的说法是“每次更新累积16条样本”，而不是每次更新具有完全相同的有效token数。

如果进一步优化，我会监控每次更新的有效completion token数量，或者按token budget动态组batch，减少长短样本差异带来的梯度波动。

## Q31：为什么使用Paged AdamW 8-bit？它和普通AdamW有什么区别？

普通AdamW通常为每个可训练参数维护一阶矩和二阶矩状态，使用FP32时优化器状态会占用较多显存。

- **一阶动量（Momentum）**：记录梯度变化的“惯性”，帮助优化器持续向正确的方向更新。
- **二阶动量（Variance）**：记录梯度变化的“幅度”，帮助优化器调整每一步的更新步长。

8-bit AdamW对优化器状态进行低比特存储，从而降低显存占用。Paged机制还可以在显存压力突然上升时利用分页机制管理状态，减少峰值显存导致OOM的概率。

不过在本项目中，只有LoRA参数需要优化，优化器状态本身已经远小于全参数训练。因此Paged AdamW的收益没有全参数训练中那么夸张，它更多是QLoRA训练栈中相对稳妥的显存保护方案。

AdamW的解耦权重衰减仍然保留，本项目权重衰减为0.01，学习率为`2e-4`。

## Q32：为什么需要gradient checkpointing？它节省的是什么，代价是什么？

正常反向传播需要保留前向过程中的中间激活值。对于长序列Transformer，激活显存通常是显存占用的重要组成部分。

Gradient checkpointing只保存部分关键节点的激活，反向传播时重新执行部分前向计算来恢复中间结果。因此它主要节省激活显存，而不是模型权重或优化器状态。

代价是：

- 增加重复计算；
- 降低训练吞吐；
- 训练时间变长；
- 某些实现可能与缓存或重入式反向传播存在兼容问题。

本项目设置`model.config.use_cache=False`，因为训练时的KV Cache与梯度检查点不兼容，也没有必要保留。脚本使用非重入式checkpoint实现，以提高与当前PyTorch训练栈的兼容性。

如果使用显存更大的V100，可以关闭gradient checkpointing换取训练速度，但需要保持其他参数不变，避免同时改变多个实验变量。

## Q33：为什么关闭packing？它不是可以提升训练效率吗？

Packing会把多条短样本拼接到一个固定长度序列中，提高token利用率，特别适合大量短样本。

本项目平均序列长度约1090，最大长度1654，而最大序列长度是1792，样本本身并不短。能够通过packing利用的剩余空间有限。

关闭packing还有两个工程上的好处：

- 每个训练序列对应一条完整、可审计的业务样本；
- 避免不同样本拼接后在attention边界和loss mask实现上产生额外复杂性。

代价是会产生一定padding和算力浪费。如果后续数据中短Query比例明显升高，我会先确认packing实现能够正确隔离样本边界和completion mask，再通过吞吐对照实验决定是否启用，而不是默认认为packing一定更好。

## Q34：为什么最大长度设置为1792？如果发生截断，会有什么风险？

这个参数来自token统计，而不是根据模型最大上下文随意设置。

数据统计结果为：

- 平均长度约1090；
- P95约1390；
- 最大长度1654；
- 超过1792的样本数为0。

之所以预留到1792，是为了给Chat Template和少量数据变化保留余量，同时控制attention的计算成本。

如果发生截断，最大的风险不是少看一些无关文本，而是可能把正确候选、候选定义或assistant标签截掉，导致训练样本语义不完整甚至标签错误。因此训练脚本默认发现超长样本就直接报错，只有显式设置允许截断后才继续。

生产中如果候选数量动态增加，也必须重新统计长度分布，不能沿用训练阶段的1792配置而静默截断。

## Q35：为什么使用`eval_loss`选择最佳checkpoint？它和严格准确率一定一致吗？

不一定一致。

`eval_loss`衡量的是正确输出token的平均负对数似然，是连续、稳定且适合训练过程比较的指标。严格准确率则是生成后的离散指标：只要JSON格式、`result`或`node_id`有一个错误，整条样本就记为错误。

可能出现两种情况：

- `eval_loss`继续下降，但生成结果已经没有变化，严格准确率不再提升；
- 模型只提高了正确答案概率，却仍然因为某个token生成错误而无法提升严格准确率。

当前训练按`eval_loss`恢复最佳checkpoint，是因为它与Trainer集成稳定，也避免频繁生成带来的训练开销。

更完善的方案是在每个checkpoint保存后进行独立生成式评估，同时记录：

- 严格准确率；
- `MATCHED`准确率；
- `NO_MATCH` Precision、Recall和F1；
- JSON有效率；
- 各实体类别Macro准确率。

最终checkpoint应根据业务主指标选择，而不是只依赖`eval_loss`。

## Q36：训练loss下降很快，你如何判断模型是在学习匹配规则，还是只记住节点ID？

我会从数据划分、对照实验和错误行为三个层面判断。

第一，Node-Holdout实验把51个节点的ID、候选记录和标签全部从训练集中移除。模型在这些节点对应的364条测试样本上仍取得96.43%的Micro准确率，说明模型不只是记忆训练节点ID。

第二，去除困难负样本后准确率从97.71%下降到85.14%。这说明性能与语义边界训练密切相关，而不是仅由输出格式或者节点频率决定。

第三，检查错误类型。当前剩余错误主要集中在业务板与网元、链路与业务等主体层级混淆，而不是随机输出节点ID。这更符合语义判别模型的错误模式。

还可以增加更严格的诊断：

- 随机替换节点ID，但保持节点文本不变；
- 打乱候选顺序，检查预测是否稳定；
- 删除节点名称，只保留定义和属性；
- 对训练节点使用全新语言表达；
- 检查模型是否偏向候选固定位置或高频节点。

如果替换随机ID后模型仍能选择与文本对应的新ID，能够更有力地证明它学习的是候选比较规则。

## Q37：如何判断LoRA rank 16是否不足或者过大？

如果rank不足，通常会出现欠拟合特征：

- 训练loss和验证loss都较高；
- 增加训练轮数后改善有限；
- 困难负样本和复杂实体类型持续表现较差；
- 梯度正常，但模型表达能力无法继续提升。

如果rank过大，可能表现为：

- 训练loss继续下降，验证loss开始上升；
- 训练节点表现很好，但Node-Holdout下降；
- 不同随机种子波动变大；
- 模型更容易记忆节点ID或语言模板；
- 训练参数量和显存上升，但指标没有显著收益。

正确实验方式是固定基础模型、数据切分、学习率、epoch和有效batch，只比较`r=8、16、32`。为了使缩放关系可比，可以保持`alpha/r`一致，例如分别使用`alpha=16、32、64`。

不能同时把rank、模型大小和训练轮数都提高，否则即使指标提升，也无法判断收益来自哪个因素。

## Q38：如何监控并处理FP16训练中的数值不稳定？

我会重点监控：

- loss是否出现NaN或Inf；
- gradient norm是否突然爆炸；
- GradScaler是否频繁跳过optimizer step；
- 学习率变化是否符合预期；
- 可训练参数和梯度的数据类型；
- 不同step的loss是否出现异常尖峰。

步骤：

- **Step 1**：看 **学习率** 是否按曲线走。如果曲线异常，先修 Scheduler。
- **Step 2**：看 **Loss** 是否有尖峰。如果有，大概率是数据问题或梯度爆炸。
- **Step 3**：看 **Gradient Norm** 是否爆炸。如果爆炸，开启或调低 **Gradient Clipping**。
- **Step 4**：看 **GradScaler** 是否疯狂跳过。如果跳过，说明上述爆炸已经严重到让 FP16 溢出了，必须调低 `lr` 或检查数据类型。

本项目采用了几项稳定措施：

- 可训练LoRA参数保持FP32；
- 使用AMP GradScaler；
- 最大梯度范数设为1.0；
- 使用5% warmup避免初始学习率冲击；
- 使用cosine scheduler平滑衰减；
- 训练前执行小规模smoke test；
- 每隔固定step记录loss、梯度范数和学习率。

如果仍然出现NaN，我会按以下顺序排查：降低学习率、定位异常batch、检查超长或非法样本、确认量化和精度配置、暂时关闭某些优化选项，最后再考虑改用支持BF16的硬件。

## Q39：如何判断模型发生了过拟合或灾难性遗忘？

过拟合主要看训练集与验证集之间的分化：

- 训练loss持续下降，验证loss开始上升；
- 同分布准确率提高，但Node-Holdout下降；
- 模型对训练模板表现很好，对新表达明显变差；
- `NO_MATCH` Precision或Recall向单一方向恶化；
- 不同随机种子的结果波动加大。

灾难性遗忘则需要额外评估基础能力。虽然QLoRA冻结了基础模型，遗忘风险低于全参数微调，但LoRA仍然可能显著改变目标层输出，使模型过度服从领域模式。

可以建立两类回归集：

- 领域回归集：节点匹配、拒识、困难负样本和未见节点；
- 通用回归集：中文指令遵循、基础理解和JSON生成。

生产部署时只加载节点匹配Adapter，其他任务不加载或切换不同Adapter，也是隔离领域适配影响的一种方式。

当前项目主要验证了领域任务和Node-Holdout，还没有系统评估通用能力保持，因此不应该宣称完全不存在灾难性遗忘。

## Q40：如果让你重新做一次训练实验，你会优先补充哪些实验？

我会按证据价值排序，而不是直接扩大模型。

第一，补充真实匿名Query测试集。当前主要结论来自受约束合成数据，真实语言噪声是最大的外部有效性缺口。

第二，完善超参数消融。在固定数据和随机种子的条件下，比较：

- LoRA rank：8、16、32；
- target modules：`q/v`、全部注意力层、`all-linear`；
- NO_MATCH过采样比例：1.0、1.5、2.0；
- 学习率：`1e-4`、`2e-4`、`3e-4`。

第三，为消融实验增加多个随机种子，报告均值、标准差和置信区间，而不是只比较单次结果。

第四，增加训练过程中每个checkpoint的生成式评估，验证`eval_loss`与严格准确率是否一致。

第五，在相同测试协议下比较1.5B、3B和7B，绘制准确率、显存、吞吐和延迟的Pareto曲线。只有这样才能更有依据地回答为什么最终选择3B。